---
import Base from '../layouts/Base.astro';
---

<Base title="Not found — FindHost">
  <h1>Not found</h1>

  <p>
    {
      /* The space before the link is written out because a newline between a word and an element is
         collapsed rather than rendered, and it is non-breaking because "or" alone at the end of a
         line reads as the end of the sentence. */
    }
    No page at that address. If you were looking for a provider, try <a href="/">the register</a> or&nbsp;<a
      href="/search/">search</a
    > — and if a provider is missing from the dataset entirely, that is a bug worth reporting.
  </p>
</Base>
