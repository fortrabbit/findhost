---
description: Hugo hosting — a single Go binary that builds a site in seconds, then serves as plain static files.
lead: Builds fast, hosts as files.
---

Hugo is a Go binary that turns content into a static directory, usually in seconds even for large sites. Hosting the output needs no runtime at all. What a provider is really offering when it names Hugo is running the build on push, which for this generator is cheap enough that almost anyone can.
