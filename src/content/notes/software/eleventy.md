---
description: Eleventy hosting — a Node build producing static files, and nothing to run afterwards.
lead: Node at build time only.
---

Eleventy runs under Node to produce static output and then gets out of the way, so the hosting requirement is a build environment with Node and somewhere to put files. There is no server component to support. Providers naming it are describing their build pipeline rather than a runtime.
