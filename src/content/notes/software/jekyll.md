---
description: Jekyll hosting — a Ruby build, static output, and a long association with GitHub Pages.
lead: The generator that made static hosting normal.
---

Jekyll builds with Ruby and outputs static files, which is why the hosting question is whether Ruby is available at build time rather than at runtime. Its long association with GitHub Pages means many sites never think about hosting at all. Elsewhere it is static hosting with a build step.
