---
description: Hosting with a command-line tool — the same capability as the API, shaped for a terminal and for scripts.
lead: What the pipeline reaches for.
---

A CLI is usually the API with a friendlier surface, and it is what a deployment pipeline actually calls. Whether the provider maintains it or a third party does is recorded on the record itself: a community tool can be excellent and it can also stop being updated the week its author loses interest, which is a different risk from an official one.
