---
description: Hosting where deploying means using the provider's web interface — an installer, a file manager, a button.
lead: Deployment as a button.
---

The control panel is how most of the web is still published: a one-click installer, a file manager, an update applied through a browser. It asks nothing of the person doing it and leaves no record of what was done, which is the trade — a site deployed this way cannot be rebuilt from a repository, because the repository is not where it lives.
